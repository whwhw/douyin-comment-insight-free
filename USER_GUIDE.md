# 抖音账号洞察 · 免费版安装与使用教程

这份教程适用于「抖音账号洞察 · 免费版」。输入一个真实抖音号，就能整理账号表现和评论需求，提取代表作品的完整口播，继续做结构拆解、金句筛选、逐段点评和二创方向分析，最后导出报告。你不需要会写代码，下面的提示词可以直接复制给 Codex。

默认最多观察 30 条作品，每条最多请求 100 条评论，最多选择 5 条代表作品转写；实际数量以获取结果为准。

## 免费版可以做什么

- 观察账号资料、作品表现，整理评论讨论点和原文证据。
- 使用高速或本地模式提取完整口播，保存 TXT 原文和 SRT 字幕。
- 查看口播结构、金句、逐段点评及二创方案。
- 生成网页报告和洞察库，导出单个账号的 ZIP 报告。

这些功能均已包含在当前免费版中，使用统一的完整报告页面。

## 1. 使用前准备

① 已能正常使用的 Codex，以及完整的免费版技能包。在 [GitHub 仓库](https://github.com/whwhw/douyin-comment-insight-free) 点击 **Code → Download ZIP** 下载。请先解压，保留全部文件，不要只复制 SKILL.md。本文以 Codex 为例。

② TikHub API Key：可以理解成读取账号与评论数据的“访问钥匙”。在 [TikHub 控制台](https://user.tikhub.io/dashboard/overview)申请并管理。

③ 高速文案提取凭据：使用火山引擎语音识别服务的 API Key。这里需要的是语音识别服务凭据，不是豆包聊天账号，也不是任意大模型 API Key。接入要求见包内 references/high-speed.md，或联系下方微信协助确认。

④ Python 3.10 或更高版本、FFmpeg 和 FFprobe：分别负责运行工具和处理音频，可让 Codex 帮你检查安装。高速提取不需要下载 Whisper 模型；只有选择本地转写时，才需要 whisper.cpp 和多语言模型。

只处理电脑里的视频不需要 TikHub；使用高速提取仍需要语音服务凭据。分析由 Codex 完成，无需额外配置用于分析的大模型 API。

## 2. 安装技能

把免费版压缩包解压，用 Codex 打开解压后的文件夹。确认里面有 README.md、SKILL.md、install.py 和 scripts 文件夹，再复制下面这段话：


```text
请帮我安装当前文件夹中的 douyin-comment-insight-free 免费版。
先阅读 README.md 和 SKILL.md，确认 edition.json 中 edition 是 free。
检查 Python 是否达到 3.10，运行 install.py，并告诉我实际安装路径。
在安装后的技能目录中创建 .venv，安装 requirements.txt。
检查 FFmpeg 和 FFprobe；先按高速文案提取模式准备。
缺少什么请用简单的话说明，并协助我安装。
保留已有配置和数据，不要输出真实密钥。
```


```text
.codex/skills/douyin-comment-insight-free
```

## 3. 配置访问密钥

安装完成后，编辑“安装目录”中的 .env 文件，不是原压缩包中的 .env.example。你可以让 Codex 帮你打开配置文件；这个文件相当于工具的设置表。


```text
TIKHUB_API_KEY=填写自己的TikHub密钥
TIKHUB_BASE_URL=https://api.tikhub.dev
ASR_PROVIDER=cloud
VOLCENGINE_SPEECH_API_KEY=填写自己的火山语音服务密钥
ASR_LANGUAGE=zh
```

把等号右边的中文说明替换为自己的密钥，保存文件。cloud 表示使用高速文案提取。FFmpeg 路径等设置可以让 Codex 协助完成：


```text
请打开免费版安装目录里的 .env，告诉我需要在哪些位置填写配置。
检查 FFmpeg 和 FFprobe 的位置，必要时填写 FFMPEG_PATH 和 FFPROBE_PATH。
我会自己填写密钥，请不要在聊天或日志中显示真实密钥。
```

## 4. 检查安装与配置


```text
请检查 douyin-comment-insight-free 是否安装正常。
使用技能目录中的虚拟环境，分别运行 doctor.py 的 collect 和 cloud 检查。
请区分“配置已填写”和“真实服务已验证”，告诉我还缺什么。
有问题请用简单的话说明原因和下一步操作，不要显示真实密钥。
```

doctor.py 是环境检查工具。检查通过只说明配置和依赖存在，不代表密钥有效、服务已开通或额度充足。建议首次先处理一小段本地视频，确认原文、字幕和状态正常，再分析完整账号。高速服务会接收音频进行识别。

## 5. 分析抖音账号


```text
使用 $douyin-comment-insight-free 分析抖音号 xxx。
使用高速文案提取，整理账号表现和评论需求，
完成代表作品的口播结构拆解、金句筛选、逐段点评和二创方向，
最后导出完整报告，并告诉我怎么打开。
如果有部分作品失败，请说明缺少哪些内容。
```

将 xxx 替换成真实抖音号，不是昵称或视频编号。

## 6. 只提取本地视频


```text
使用 $douyin-comment-insight-free，处理我提供的本地视频。
使用高速文案提取，导出 TXT 原文和 SRT 字幕。
不要采集抖音账号，完成后告诉我文件保存在哪里。
```

## 7. 查看历史与报告


```text
使用 $douyin-comment-insight-free，查看抖音号 xxx 的历史报告，不重新采集，也不重新转写。

如果需要基于旧数据继续分析，可以另发：
使用 $douyin-comment-insight-free，复用 xxx 的历史数据继续分析，不调用采集和云端转写。
```

最方便的方式是让 Codex 给你可点击的报告路径。HTML 是用浏览器打开的网页文件，TXT 是原文，SRT 是可以导入剪辑软件的字幕。


```text
workspace/site/comment-insight-index.html：全部账号报告入口
workspace/site/抖音号.html：这个账号的报告
workspace/runs/：每次分析及其原始证据
导出的 report.zip：解压后打开 index.html
```

分享报告时发送完整 ZIP，不要只发一个 HTML 文件。报告中包含评论原文，发送前请检查内容。部分封面等资源需要联网显示。

## 8. 出错怎么办


```text
刚才运行失败了，请检查具体原因。
区分是环境依赖、TikHub 配置、语音服务权限或额度、网络，还是媒体文件问题。
用简单的话告诉我卡在哪一步，并协助修复。
保留已完成结果，尽可能复用成功的采集和转写。
不要自动切换转写服务；删除数据或覆盖配置前先告诉我。
不要显示真实密钥。
```

## 9. 搭配使用与联系

推荐搭配之前分享的「运营助手」：先用本技能研究对标账号，再把报告中的评论需求、内容思路和二创方向交给运营助手，结合自己的账号定位规划后续选题。为得到更贴合自己的建议，请补充目标观众、可用素材和拍摄条件。

安装、配置或使用过程中有任何问题，可以联系微信 **ai_coder_wang**，备注「免费版安装」。反馈时说明电脑系统、卡在哪一步，并提供隐藏密钥后的报错截图。

## 10. 常见问题

**Codex 找不到技能怎么办？**先让它检查安装器输出的目录中是否有 SKILL.md，并明确使用 $douyin-comment-insight-free。如果仍未识别，可以重新打开 Codex 会话后再试。

**已经安装旧版怎么办？**下载当前版本，让 Codex 从新包中执行 `python3 install.py --upgrade`。Windows 可用 `py -3 install.py --upgrade`。更新会备份旧版本并保留已有配置与数据；已有 `.env` 的转写模式不会自动改写。

**不想使用高速服务怎么办？**可以明确选择本地转写。让 Codex 按 README 安装 whisper.cpp 和多语言模型，配置 WHISPER_MODEL_PATH，再将 ASR_PROVIDER 改为 local 并运行本地环境检查。只改这一个开关而不准备模型，无法完成转写。

**报告缺少结构点评或二创怎么办？**先检查对应作品的转写是否完整。无声、截断或重复识别等问题会标记为待复核；没有合格原文时，系统不会编造深度分析。可以先查看已完成的账号和评论分析。

**想获取最新作品怎么办？**明确告诉 Codex“重新采集并分析这个账号”。这与只查看历史报告不同，可能产生新的接口调用。

**更新技能会丢数据吗？**请从新下载的免费版包中按 README 使用 install.py --upgrade。安装器会备份旧版本并保留配置、虚拟环境和工作数据，不要手动删除旧目录后重装。

## 11. 当前版本说明

当前版本为 1.1.0。免费版 17 项适用自动化测试通过；浏览器已检查桌面和 390px 窄屏布局、详情弹窗、评论证据、空数据及导出报告。窄屏检查不等于手机实机测试。本地转写与小规模在线采集已在 macOS 验证；高速提取完成了接口模拟测试，真实云端识别效果、速度与计费，以及 Windows 实机仍待验收。

转写可能有错字；作品排名仅代表本次账号样本中的相对表现。报告用于研究和创作参考，不代表播放、涨粉或成交保证。

## 想进一步得到行动建议？

如果希望在分析结果基础上，进一步获得有评论依据的尝试方向、验证步骤和衡量指标，可以体验专业版的**行动建议分析**。两版公共页面和默认分析数量一致。

了解体验方式或使用中遇到问题，可联系微信 **ai_coder_wang**，备注「账号洞察」。
